syntax = "proto3";
package user_service;

service UserService {
    rpc GetUser(UserId) returns (User);
}

message User {
   UserId id = 1;
   string name = 2;
   string mail = 3;
}

message UserId {
    string value = 1;
}